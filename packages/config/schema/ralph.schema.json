{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "Pointer to this JSON Schema, so an editor completes and validates the file.",
      "type": "string"
    },
    "simpleUrlCrawlRules": {
      "description": "URLs to crawl without scripted steps, each with its own normalize rules. Flows that need interaction are captured by Playwright tests instead.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/SimpleUrlCrawlRule"
      }
    },
    "hostOverrides": {
      "description": "Host rewrites applied when the Web navigation graph is built, for when the crawl and the tracker run on different hosts: the crawler visits `dev.web.halfmore.co`, the tracker reports from `halfmore.co`, and the graph has to record the host the tracker reports for the two to join. The first rule whose matcher matches wins and the rest are skipped, so a rewritten host is never rewritten again. The crawl itself is untouched - only the host the graph records changes.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/HostOverrideRule"
      }
    },
    "graphUrlNormalizeRules": {
      "description": "Normalize rules to apply, in order, to every recorded URL when the Web navigation graph is merged, e.g. collapsing `/product/42` and `/product/43` onto one `/product/:id` node. Covers Playwright recordings and simple URL crawls alike, so both merge onto the same nodes. Applied after `hostOverrides`, so matchers see the host the graph records. Keep the path rewrites in line with `urlAnalysisNormalizeRules`, or tracker data will not join the graph nodes.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/UrlNormalizeRule"
      }
    },
    "urlAnalysisNormalizeRules": {
      "description": "Normalize rules to apply to the URLs for the data aggregation, in order. E.g. user identifying query params to the URL should be irrelevant for the analysis.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/UrlNormalizeRule"
      }
    },
    "variantRules": {
      "description": "Variant rules to apply to the specific URL. E.g. when A/B testing multiple versions of the same page, the variant rules identify the versions so they can be aggregated.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/VariantRule"
      }
    },
    "screenSizes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ScreenSize"
      },
      "description": "Screen sizes to run the crawl / capture screenshots at."
    }
  },
  "required": [
    "screenSizes"
  ],
  "additionalProperties": false,
  "description": "The crawl configuration a `ralph.jsonc` declares.",
  "definitions": {
    "SimpleUrlCrawlRule": {
      "type": "object",
      "properties": {
        "scenario": {
          "$ref": "#/definitions/SimpleUrlCrawlScenario"
        }
      },
      "required": [
        "scenario"
      ],
      "additionalProperties": false,
      "description": "A crawl that starts from one URL and follows links, without scripted steps. Use Playwright tests for flows that need interaction."
    },
    "SimpleUrlCrawlScenario": {
      "type": "object",
      "properties": {
        "name": {
          "description": "A label for the scenario, e.g. `Product listing`.",
          "type": "string",
          "minLength": 1
        },
        "startUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL the crawl starts from. Should be accessible from the crawler."
        },
        "dataFollowIgnoreRules": {
          "description": "Follow ids to skip on matched URLs, so the crawler doesn't keep re-visiting nav-bar/footer links that appear on every page.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/DataFollowIgnoreRule"
          }
        }
      },
      "required": [
        "startUrl"
      ],
      "additionalProperties": false,
      "description": "One URL to start crawling from, and which follow ids to skip. The URLs it reaches are normalized by the top-level `graphUrlNormalizeRules`."
    },
    "DataFollowIgnoreRule": {
      "type": "object",
      "properties": {
        "matcher": {
          "$ref": "#/definitions/UrlRuleMatcher"
        },
        "ignore": {
          "$ref": "#/definitions/Condition"
        }
      },
      "required": [
        "matcher",
        "ignore"
      ],
      "additionalProperties": false,
      "description": "Follow ids the crawler should not follow on the URLs a matcher matches. Without this, follow ids that appear on every page (nav bars, footers) get clicked from every node, so the crawler keeps re-visiting the same destinations."
    },
    "UrlRuleMatcher": {
      "type": "object",
      "properties": {
        "host": {
          "description": "Matched against the URL host, e.g. `dev.web.halfmore.co`, or `localhost:3000` when the URL carries a port.",
          "allOf": [
            {
              "$ref": "#/definitions/Condition"
            }
          ]
        },
        "path": {
          "description": "Matched against the URL path, e.g. `/product/42`.",
          "allOf": [
            {
              "$ref": "#/definitions/Condition"
            }
          ]
        },
        "queryParams": {
          "allOf": [
            {
              "$ref": "#/definitions/QueryParamCondition"
            }
          ]
        }
      },
      "additionalProperties": false,
      "description": "Which URLs a rule applies to. Every field given has to match; an empty matcher matches all."
    },
    "Condition": {
      "description": "How a string is tested. A bare string - or a list of them, any of which may match - is an exact match; `exact` / `prefix` / `suffix` / `contains` / `regex` / `isSet` name another test; `any` / `all` / `not` combine them.",
      "anyOf": [
        {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "A single string, or a list in which any member matching matches."
        },
        {
          "type": "object",
          "properties": {
            "exact": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "A single string, or a list in which any member matching matches."
            }
          },
          "required": [
            "exact"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "prefix": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "A single string, or a list in which any member matching matches."
            }
          },
          "required": [
            "prefix"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "suffix": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "A single string, or a list in which any member matching matches."
            }
          },
          "required": [
            "suffix"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "contains": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "A single string, or a list in which any member matching matches."
            }
          },
          "required": [
            "contains"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "regex": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "A regular expression source string, or a list in which any member matching matches."
            }
          },
          "required": [
            "regex"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "isSet": {
              "type": "boolean",
              "const": true,
              "description": "Matches whenever the string exists at all."
            }
          },
          "required": [
            "isSet"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "any": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Condition"
              }
            }
          },
          "required": [
            "any"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "all": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Condition"
              }
            }
          },
          "required": [
            "all"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "not": {
              "$ref": "#/definitions/Condition"
            }
          },
          "required": [
            "not"
          ],
          "additionalProperties": false
        }
      ]
    },
    "QueryParamCondition": {
      "description": "How a URL's query params are tested. `{ key, value }` matches when some single param has a key and a value that both match - omit `value` to test only that the key is there. `any` / `all` / `not` combine those, so `not` means no param satisfies it.",
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "key": {
              "$ref": "#/definitions/Condition"
            },
            "value": {
              "allOf": [
                {
                  "$ref": "#/definitions/Condition"
                }
              ]
            }
          },
          "required": [
            "key"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "any": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/QueryParamCondition"
              }
            }
          },
          "required": [
            "any"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "all": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/QueryParamCondition"
              }
            }
          },
          "required": [
            "all"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "not": {
              "$ref": "#/definitions/QueryParamCondition"
            }
          },
          "required": [
            "not"
          ],
          "additionalProperties": false
        }
      ]
    },
    "HostOverrideRule": {
      "type": "object",
      "properties": {
        "matcher": {
          "$ref": "#/definitions/UrlRuleMatcher"
        },
        "updatedHost": {
          "type": "string",
          "description": "The host to record instead, e.g. `halfmore.co`. It replaces the host whole, so carry the port when the graph should have one."
        }
      },
      "required": [
        "matcher",
        "updatedHost"
      ],
      "additionalProperties": false,
      "description": "The host the graph records for the URLs a matcher matches, in place of the one the crawler visited."
    },
    "UrlNormalizeRule": {
      "type": "object",
      "properties": {
        "matcher": {
          "$ref": "#/definitions/UrlRuleMatcher"
        },
        "transform": {
          "$ref": "#/definitions/UrlTransformRule"
        }
      },
      "required": [
        "matcher",
        "transform"
      ],
      "additionalProperties": false,
      "description": "A transform to apply to the URLs a matcher matches."
    },
    "UrlTransformRule": {
      "type": "object",
      "properties": {
        "updatedPath": {
          "description": "Update the path of the URL to the given value.",
          "type": "string"
        },
        "queryParams": {
          "description": "Query param edits, applied in order. `set` rewrites the value of every param whose key matches, and appends the param when the key is a plain string and no param carries it; `remove` drops every param whose key matches.",
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "set": {
                    "$ref": "#/definitions/Condition"
                  },
                  "value": {
                    "type": "string"
                  }
                },
                "required": [
                  "set",
                  "value"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "remove": {
                    "$ref": "#/definitions/Condition"
                  }
                },
                "required": [
                  "remove"
                ],
                "additionalProperties": false
              }
            ]
          }
        },
        "removeOtherQueryParams": {
          "description": "If set, drop every query param that `queryParams` did not touch.",
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "description": "What to rewrite on a URL the matcher matched."
    },
    "VariantRule": {
      "type": "object",
      "properties": {
        "matcher": {
          "$ref": "#/definitions/UrlRuleMatcher"
        },
        "variants": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Variant"
          }
        }
      },
      "required": [
        "matcher",
        "variants"
      ],
      "additionalProperties": false,
      "description": "The variants to look for on the URLs a matcher matches."
    },
    "Variant": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The id this variant is aggregated under."
        },
        "queryParams": {
          "description": "The query params that identify this variant, as exact key / value pairs. Omit it for the variant a URL carrying none of the others falls back to.",
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false,
      "description": "One version of a page, identified by its query params."
    },
    "ScreenSize": {
      "type": "object",
      "properties": {
        "width": {
          "type": "number",
          "description": "Viewport width in CSS pixels."
        },
        "height": {
          "type": "number",
          "description": "Viewport height in CSS pixels."
        }
      },
      "required": [
        "width",
        "height"
      ],
      "description": "A rendered screen size: width x height in CSS pixels. Used both for the crawl capture sizes and for the size a usage event was recorded at."
    }
  },
  "$id": "https://raw.githubusercontent.com/ralphralphai/ralph-js/main/packages/config/schema/ralph.schema.json"
}
