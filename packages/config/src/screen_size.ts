// A rendered screen size: width x height in CSS pixels. Used both for the
// crawl capture sizes and for the size a usage event was recorded at.
export type ScreenSize = {
  width: number;
  height: number;
};
